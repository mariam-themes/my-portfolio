'use client';

/**
 * Reserved "About" section.
 *
 * Intentionally left empty — the designer's full resume/CV will be authored
 * separately and dropped into this slot (which already sits in the correct
 * position within the homepage section order). Do not add placeholder copy.
 */
export default function AboutSection() {
  return <section id="about" className="relative z-10 scroll-mt-24" aria-label="About" />;
}
